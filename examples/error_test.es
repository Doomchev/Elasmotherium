// Class "Unknown" is not found.
Unknown a;
//
Unnknown function() -> 0;
//
Int function(Unnknown value) -> 0;
//
function(Int i, Unnknown value) -> 0;

// Unexpected ;
a;
//
a=;

// Object "unknown" is not found
unknown = 0;

// Unexpected ==
Int a == 1;

// Function "unknown" is not found
unknown();

// Unexpected =
if(0 = 1) print("");
